const app = require('express').Router(); 
// const bcrypt = require('bcrypt');
// const User = require('../../models/User');

// the url is localhost:3001/api/users
app.get('/', (req, res) => {
	res.send('Hello this the route for user DATA');
	});








module.exports = app;